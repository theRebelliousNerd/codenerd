# Synthetic .code-audit.md Template (codeNERD)

`arch-propose-auditor` writes this instead of a real code audit. Writers consume it like a
code-audit artifact. The synthetic flag suspends `file:line` for pure greenfield targets.

```markdown
# {Feature} — Synthetic Code Audit Report

> **⚠ Synthetic audit — no source package scanned for the target.** Generated by
> arch-propose-auditor from research dossiers. `file:line` suspended for greenfield
> foundation docs and planned-only sections. ENFORCED for adjacent existing code and
> all wiring/cross-cutting claims.
>
> Generated: {YYYY-MM-DD}
> Research sources: {scout paths}
> Interrogation verdict: {READY / NEEDS_WORK}
> Target tier: {1/2/3}
> Expand mode: {pure_new_subsystem / partial_corpus}

## 1. Package Structure (Planned)

**Primary planned package:** `internal/{feature}/` (does not yet exist)

| File (planned) | Purpose | Design source |
|---|---|---|
| `internal/{feature}/{file}.go` | {role} | candidates/{feature}.md §{N} |

**Adjacent packages gaining hooks** (exist today):

| Package | Integration | Existing file:line |
|---|---|---|
| `internal/{adjacent}/` | {hook nature} | `path:line` |

## 2. Planned Types / Interfaces

Target Go signatures (not implemented).

## 3. Mangle Surface (Planned)

| Predicate / Decl | Role | Policy impact |
|---|---|---|
| `Decl foo(...)` | {why} | permitted / next_action / none |

## 4. VirtualStore / Tools (Planned)

| Route or tool | Owner package | Notes |
|---|---|---|

## 5. Shard Impact (Planned)

| Shard type | Name | Registration point |
|---|---|---|

## 6. Prompt / JIT Surface (Planned)

| Atom id / category | Selection keys | Mandatory? |
|---|---|---|

## 7. Fact Flow Placement

Where this feature sits in:
user_intent → kernel → next_action → VirtualStore → articulation

## 8. Constitutional Safety

Danger classes, Dreamer implications, default-deny coverage.

## 9. Invariants

Hard invariants that must hold once implemented.

## 10. Test Plan Anchors

Pointers for TESTING-STRATEGY.md (unit / race / Mangle / campaign).

## 11. Risks & Open Questions

## 12. VERBATIM-FOR Blocks

### VERBATIM-FOR:IMPLEMENTED_SPEC
{blocks writers must paste unchanged}

### VERBATIM-FOR:02-CURRENT-STATE
{adjacent inventory only}
```
