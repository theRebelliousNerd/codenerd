# Compatibility route: pre-implementation checklist

The Vectryx source package called this the greenfield checklist. codeNERD also applies the workflow to substantial additions inside existing subsystems, so the active codeNERD contract is [pre-implementation-phase-checklist.md](pre-implementation-phase-checklist.md).

Use that checklist as the single source of truth; this file exists only to preserve source-package reference closure.
