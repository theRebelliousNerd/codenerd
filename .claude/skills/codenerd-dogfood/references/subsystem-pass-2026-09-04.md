# Subsystem first pass — 2026-09-04

Five thorough read-only sweeps (kernel/policy/core; the turn loop; long-horizon
subsystems; effect and memory subsystems; the control surface), each verified
at the cited lines and each running its packages' targeted tests. This is the
punch list; items are marked as they land. Ledger context:
`component-ledger.md` (2026-09-03 section) and `composition-map-2026-09-03.md`.

## Landed the same day

| # | Finding | Commit |
|---|---|---|
| 1 | Checkpoint verdict read from the kernel (`checkpoint_verdict/4` declared); previously parsed the stripped surface text and failed every checkpoint | 16e830d8 |
| 2 | New-source hollow rule scoped to this turn; existence check restored in `recordGoFileCreations`; `build_state` asserted from build verification | 16e830d8 |
| 3 | Production VirtualStore adapters implement the interactive gate (Dreamer + validators now run outside the TUI) | 16e830d8 |
| 4 | ConstitutionGateShard owns only executive-prefixed `pending_action`; executor uses `exec-`; `permitted_action` gated on a running router and pruned | 16e830d8 |
| 5 | Meta reasoning cache keyed by conversation and bounded; `SetStrategicContext` locked | 16e830d8 |
| 6 | Six granted tools had no `safe_action` (web_search, web_fetch, context7_fetch, get_impacted_tests, run_impacted_tests, apply_edits); `apply_edits` mapped in the gate | 72990135 |
| 7 | Decomposer retypes file tasks against the filesystem | d21bc4ce |
| 8 | Campaign phase 0–1 output (turn evidence, safe-action projection, one project-shape source) | 755c5732 |
| 9 | `nerd sessions load` printed a flag that does not exist | 5479bdcf |
| 10 | Dreamer projected `/critical_path_hit` for *edits* under `internal/core`, `internal/mangle`, `cmd/nerd`, `.nerd`, `.git`, so `panic_state(_, "critical_path_missing")` blocked every write there. Surfaced the moment item 3 put the Dreamer on the executor path (run 23: 38 tool calls, every edit refused, hollow-success guard caught it). The schema defines critical paths as "never removed recursively"; modification now hits only for `.git`, and the prefix match is segment-aware (`internal/corex` no longer matches) | (this batch) |
| 11 | `TestAddFactsContext_HonorsDeadline` waited a fixed 2 ms for a 1 ns timer; on Windows the timer fires at ~15 ms resolution. Now waits on `ctx.Done()` | (this batch) |
| 12 | Registry↔policy parity test (`internal/tools/catalog_policy_parity_test.go`, built by codeNERD, run 22). It immediately found four more granted-but-ungated tools: `research_cache_get/set/stats/clear`. get/set/stats gained `safe_action`; `clear` stays denied on purpose and is the test's one listed exception | (this batch) |
| 13 | Campaign file tasks: target from `Artifacts[0]`, else the first exact write-set entry (relativized), else the description; empty target is an error before any shard call; a directory or missing file never counts as written; pathless mutating tasks retyped to `/research` at plan time (codeNERD, run 24) | (this batch) |

| 14 | `-i` mode: per-turn `--timeout`, EOF exits, spawn by verb with the shared hollow guard and root-write sweep (run 25; spawn argument order corrected by hand) | b4374849 |
| 15 | One session identity minted at boot (`Cortex.SessionID`, honors `BootConfig.SessionID`), propagated to executor, spawner and clones; Ouroboros registry wired on every path, nil-safe (run 26) | 4171060f |
| 16 | `ConfigFactory.Generate` validates; inert `ToolLoop`, `Model`, `Safety`, `Workspace` deleted; fixtures moved to canonical policy names (run 27) | dd807334 |
| 17 | CLI flags: one `--verbose`, `--dry-run`/`--trace-api` deleted, `--dump-kernel` wired, `--disable-system-shard` persistent, dead `config.yaml` block removed (run 28; tests by hand) | a3ff2192 |
| 18 | `TaskVerifier` fails closed: verifier outage → stored as failed, retries stop, `ErrVerificationUnavailable` (run 29) | d7d2695a |
| 19 | Shadowed `tactile_router`/`campaign_runner` factories deleted from `registration.go`; `ShardManager.HasShardFactory` (run 30) | 3e7940ff |

| 20 | Memory read-back on the executor path (`MemoryHydrator` capability), `atomsJSON` populated, `turn_cost/6` declared and asserted per turn (run 31) | 5aaaf96c |
| 21 | Dead `performSystemBootLegacy` (~1,045 lines) and its two private helpers deleted (run 32; file removal by hand, `delete_file` is human-gated) | 5bcd12f8 |
| 22 | Legacy YAML `Config`/`Load` surface deleted; `ValidateCoreLimits`/`EnforceCoreLimits` moved onto `CoreLimits` (run 33; stubs removed by hand) | 990d3e39 |
| 23 | **Live probe found item 20 inert in production:** the executor's store is the factory adapter, which did not implement the hydrator capability (unit test used a fake). `turn_cost` read cross-process project totals: 455K "prompt tokens" for a ~20K turn while three fix runs were active. Adapter delegates; per-session counts via `usage.WithSessionID`/`Tracker.SessionTokens` | 37ee811b |
| 24 | TUI: dead clarifier accumulation on a value receiver removed; knowledge guard latches on the Update thread; continuation steps parented on the shutdown context (run 35 + hand) | c10f6ea0 |
| 25 | Retrieval embedding tier wired through `nerd retrieve` and the TUI seed; seed summary names the tier (run 36) | f57ec806 |
| 26 | `run_command` default timeout is toolchain-aware (600 s for go/cargo/npm/make/pytest build+test) — the flat 60 s was why runs 22, 24, 31, 36 "timed out with no output" on `go test`. Cancellation kills the process tree (`taskkill /T` / process group); 17 orphaned greps, some 3 days old, were found | 2c132b51 |
| 27 | Autopoiesis accessors for the tool generator and concrete Ouroboros loop (groundwork for item 28) | 7318e86b |

| 28 | Read-only turns earn a turn verdict (`checkHollowSuccess` returned before asserting evidence for non-tool verbs, so every `/explain` was `/unverified`); measured, never failed for hollowness | 30094744 |
| 29 | Test-only chat adapters and migration helper deleted, 1,012 lines (run 38) | (see log) |
| 30 | Cortex accessors: worker/planner clients, tool generator, Ouroboros loop, MCP store (run 37's surviving half) | 202bf809 |
| 31 | `core_limits` validated at load, presence-aware; dead `EnforceCoreLimits` deleted (run 40 — first run to complete its own `go test` verification unaided since item 26) | 60e13933 |
| 32 | `nerd campaign start` boots through the Cortex; `buildCampaignOrchestratorConfig` is one pure construction site; ToolPregenerator always built (run 39) | 6b6eefa3 |

## In flight (briefs written)

- Factory honors `per_shard_facts` (single catch-all shard when off) — run 42 (`internal/system`).
- Campaign shard manifest owns the whole campaign fact family + contract test — run 43 (`internal/shards`, `internal/campaign`).
- Campaign resume through the Cortex + failed/blocked resume semantics — run 41 lost its core logic to a revert of mine; re-dispatch after run 43 (same package).

**Live end-to-end proof (08:11, `nerd campaign start "Audit internal/retrieval …" --type audit --timeout 20m`, binary 6b6eefa3):** booted through the Cortex ("Tool pregenerator initialized" replaced "pending (requires Ouroboros)"), planned 4 phases / 12 tasks, completed phases 1–2 and 5 of phase 3's tasks (11/12) with per-task artifacts under `.nerd/campaigns/a19dd99f/artifacts/`, then paused cleanly at the ceiling with the resume hint. Exit 1 by design.

Final state: `/paused`, 11/14 tasks complete; phase 3 grew from 3 to 5 tasks because a corrective re-plan re-emitted two audits with "in internal/retrieval" appended and the duplicate filter compares normalized descriptions exactly (`decomposer_planning.go` ~701) — near-duplicates cost two full task runs. Open, low.

**Resume half (08:47, `nerd campaign resume --timeout 20m`, same binary):** resumed the paused campaign, closed phase 3, ran phase 4's three tasks and reported "Campaign completed successfully" (exit 0). But the deliverable was hollow: the ranked-risk report said "No input content supplied for review. No findings to rank." (314 bytes, written to a new `audit/` directory in the repository root), and neither the nemesis check nor the "verify every item links to file plus symbol" task failed it; phase 4 persisted no artifacts at all. The nine upstream audit artifacts (8–19 KB each, line-anchored) were never given to the phase 4 tasks. Item 34, open: upstream artifact propagation into dependent tasks, and verification tasks that must fail a findings-free report when upstream findings exist. Brief: `brief_campaign_context.txt`.

Item 33 landed as runs 42/43: complete campaign manifest + contract test (70d85b08) is the real fix; the literal-count guards that had been red since 6b6eefa3 fixed in 733eccdc. **c52bf618 (honor `per_shard_facts=false` with one catch-all shard) was reverted the same morning:** probe 3 at 09:07 booted in 114 s (vs 12–16 s) and one turn took 7m53s — the performance log shows every `kernel.evaluate` at 33–40 s and `HydrateKnowledgeGraph` at 70 s, against 0.23–0.25 s and 1.2 s on the seven-shard probe at 07:45 under similar load. One store puts every large fact family (9.4K `knowledge_link`, world facts) under every rule that joins it. The domain split stays on regardless of the flag (4d3553c5); the boot line records the flag as not honored. **Probe 4 (09:53, binary 4d3553c5):** boot 15.9 s, seven shards, one tool call, correct answer in 1m50s with a fix run sharing the provider. Open, low: make single-store evaluation affordable or delete the flag; two `BootCortexWithConfig` calls in one test process hung the package (one boot per test passes in ~3 s). Item 34 is run 45 (`brief_campaign_context.txt`; run 44 timed out on the single-shard binary without changing a file).

**Item 34 landed (54ba9912, run 45, 10:4x):** `internal/campaign/upstream_context.go` builds a "## Upstream findings (durable artifacts)" section from completed hard/artifact-dependency and earlier same-phase `/doc` artifacts (12 KB per artifact, 48 KB total, newest phase first) and `buildTaskInput` appends it to every research, file, test, refactor and shard-spawn task; `executeVerifyTask` now fails when the report it checks is under 1 KB or carries "no findings" markers while upstream artifacts contain findings. codeNERD wrote the change and its five tests; its truncation fixture used a 100-byte cap on 75-byte artifacts, fixed by hand (40 bytes). The resume brief was split into three single-package runs because two-package briefs exceed a run's budget: run 46 `PrepareResume` + persisted `block_reason`/`resume_count` (`brief_resume_prepare.txt`, `internal/campaign`), run 47 resume boots through the Cortex and deletes `newCampaignLLMClients`/`campaignLLMClients` (`brief_resume_boot.txt`, `cmd/nerd`), then a small run for status selection (paused > active > failed-blocked, `--retry-failed`) and the `PrepareResume` call.

**Runs 46 and 47 landed (d5dd27da, 00ce937f, ~11:30):** `PrepareResume` resets failed/in-progress/blocked tasks below the attempt cap, refuses when the current phase has nothing resumable, and the block reason is now written to the campaign JSON; `nerd campaign resume` boots through `GetOrBootCortex` + `buildCampaignOrchestratorConfig` exactly like start (233 lines of private boot deleted, a go/parser guard pins it). Run 46's verification failed only because run 47 was deleting the resume-path `OrchestratorConfig` literal that `internal/campaign`'s three literal-count guards count across `cmd/nerd` — a cross-package coupling that makes "disjoint packages" insufficient for concurrency when one package's tests scan the other's sources; verified run 46 by hand and lowered the guards 4→3 (they went 5→4 in 733eccdc for the same reason). Run 48 (`brief_resume_select.txt`) wires status selection and the `PrepareResume` call.

**Run 48 landed (9e77ffd3, ~12:10):** resume loads every persisted campaign and picks newest paused > active > failed-with-block-reason > (`--retry-failed`) any failed, calls `PrepareResume` after `SetCampaign`, persists the re-armed state; `findCampaignByStatuses` deleted. Its full-suite check surfaced two test-only regressions left by earlier runs (875b12ea, hand): the northstar guardian-wiring guard still read the `session_boot.go` that run 32 deleted, and the system gate test's hand-rolled `gateStubKernel` lacked `KernelTransactor` (replaced by `typestest.NewMockKernel()`). Lesson: a run that deletes a file must grep `_test.go` across the tree for path literals, not just callers.

**Third live campaign pass (11:13, `nerd campaign start "Audit internal/retrieval …" --type audit --timeout 20m`, binary 875b12ea, campaign 5a2f4c8d):** item 34 proven at three depths from the campaign log — `injected 1 upstream artifacts (3514 bytes)` same-phase, `2 … (10260 bytes)` cross-phase, `3 … (19023 bytes)`. **Item 35, found by that run (the self-sharpening loop working as designed):** the reviewer answered the shard-validation checkpoint with a well-formed control packet carrying `checkpoint_verdict("Retrieval Scaffold Inventory", /pass, "…", 92)` (llm_io 63863) and the checkpoint logged `Checkpoint FAILED … missing or malformed checkpoint_verdict/4` three times, burning every retry. Three subsystems hold three versions of one contract: the checkpoint's prompt example omits the trailing period (`checkpoint.go:288`, `:388`, `assault_tasks.go:345`); the articulation validator drops any update without one (`emitter.go:447`) into a `Warnings` slice nothing reads or logs, so the drop is silent; the executor's `mangle_updates` allowlist (`executor.go:1375`) never listed `checkpoint_verdict`, so even a correct atom died there; and the checkpoint's raw-envelope parser rejects the period the validator requires. Every phase checkpoint has failed closed since the structured verdict was built — the "reviewer PASS in prose" the campaign then ignores was masking it. Fixes: allowlist entry by hand with a test that also pins `permitted/3` unassertable (c202a520); run 49 `brief_verdict_contract.txt` (prompt period, parser tolerance, retract stale verdicts before the reviewer spawns so a task shard cannot pre-approve its phase); run 50 `brief_articulation_warnings.txt` (log the dropped updates). **Item 36, from the same run:** `turn_cost … prompt=0 completion=0` on every campaign-spawned executor. `perception.trackUsage` records only when the context carries a tracker (`usage.FromContext`), ten CLI entry points attach it by hand, and `cmd_campaign.go` start/resume never did — so campaigns and every shard they spawn meter nothing, and the per-turn denominator the composition campaign is built on reads zero there. Fix is one seam, not an eleventh call site: `sessionLLMAdapter` attaches the Cortex tracker when the context lacks one (`brief_usage_seam.txt`, run 51, `internal/system`); the ten hand-tagging sites then become deletable. Also noted: after three checkpoint failures the orchestrator logs `exhausted 3 checkpoint attempts; advancing with UNVERIFIED checkpoint` and completes the phase — a deliberate no-deadlock rule, acceptable once verdicts flow, but the phase record should carry the unverified flag (open, low); and rolling-wave refinement between phases took 7m42s (11:24:00→11:31:42) — a third of the 20 m budget for one re-plan (open, measure).

**Item 37 (from the persisted plan):** the plan is a straight chain (phase N depends only on N−1) and `collectDepCandidates` walks one level, so the phase-4 report would see phase 3's artifacts only and never the domain mapping's seven findings — `brief_transitive_upstream.txt` (run 52, after run 49 leaves `internal/campaign`). The decomposer again targeted the report at repo-root `audit/retrieval_risk_report.md` (open: campaign documents should default under the campaign's artifacts directory unless the goal names a path).

**Items 35–37 landed (~12:35):** verdict contract 43f9e043 (run 49) + allowlist c202a520 (hand) + dropped-update logging 66d4a79a (run 50); metering seam 0e0905d3 (run 51); transitive upstream 11faf065 (run 52). Also deleted the usage tracker's "legacy raw-string key kept for one release" fallback — zero producers, a shim (c4798da7). Run 51's full-suite check saw `internal/logging TestExportAuditFacts` fail once; it passes in isolation (flaky under concurrent load, open low). Campaign 5a2f4c8d resumed at ~12:40 on binary 0e0905d3 to prove: checkpoints pass on merit, `turn_cost` non-zero for spawned shards, transitive injection into the phase-4 report, report not hollow.

**Resume results so far (12:02–12:15):** transitive injection live — `injected 4 of 4 upstream artifacts from 2 phases (24981 bytes)` on phase 3, `4 of 4 … from 3 phases` on each phase-4 task, `5 of 5 … 33868 bytes` on the last. **Item 38, found by the phase-3 `/tests_pass` checkpoint:** "3833 passed, 37 failed" — every failure `[build failed]` on the sqlite-vec cgo bindings, `'sqlite3.h' file not found`. The tactile `DirectExecutor` passes only allowlisted parent env keys (`direct.go:268`); the parent shell has no `CGO_CFLAGS` (it lives in Go's `go env -w` file, found via `APPDATA`, which is not allowlisted), and `internal/store/init_vec.go` imports the bindings under plain `//go:build cgo`. Reproduced from a shell with `env -i` and the allowlist keys. So every checkpoint or shard verification that runs `go build`/`go test` through the tactile executor has failed on the toolchain, not the code; only the VirtualStore `run_command` path applies `build.GetBuildEnv` (`virtual_store_actions.go:1192`). Same shape as item 36: attach at the seam, `brief_tactile_build_env.txt` (run 53).

**12:20:57 — first checkpoint to pass on merit in any campaign this session:** `runShardValidationCheckpoint: reviewer approved phase=Correctness and Safety Audit` / `Checkpoint PASSED: /shard_validation`; kernel log shows retract-before-spawn (nothing stale), `Query: predicate=checkpoint_verdict returned 1 results`, retract after read. Item 35 proven live. **Item 36 half-proven:** `.nerd/usage.json` now records `by_session["session-1788537770966983600"] = {input 2071403, output 79707}`, so the adapter seam meters — but `turn_cost` still logs `prompt=0 completion=0` because `snapshotTurnUsage` reads the tracker from the executor's own context, which the campaign never tagged. Metering and readback are two hops; `brief_executor_tracker.txt` (run 54) gives the executor its own tracker (set by the factory next to `SetSessionID`, copied by `CloneForTask`). **Item 39 (observed, open):** the phase-5 report task got `injected 4 of 8 upstream artifacts from 4 phases (45604 bytes, budget 49152)` — the four nearest artifacts consumed the budget and the domain-mapping and lifecycle findings were cut. Newest-first is the right priority for a next task but a final report needs spreading: when considered × per-artifact cap exceeds the budget, lower the per-artifact cap (floor ~2 KB) so every phase contributes a head. Also visible again: the re-plan's near-duplicate task pairs in phase 4 ("Research correctness…" twice).

**12:22 — second pause at the ceiling, mid-report:** the phase-5 document task was 1 min into attempt 2 (attempt 1: coder shard hit the deadline-aware forced final answer; fallback direct-LLM also deadline) when the 20 m ran out; task reset to pending, clean pause, resumed again at 12:24. **Item 40:** the rolling-wave re-plan added a duplicate document task ("Assemble short ranked risk report…", `/task_5a2f4c8d_4_2`) with no target path; it failed twice with `file task … has no target path` and then entered the diagnostic-repro loop — the run-24 rule that retypes pathless mutating tasks to `/research` lives only in the decomposer and the re-plan path does not apply it. Also the second resume's `Resuming campaign … (status /active)` line prints the status after the paused→active flip, so it always says active (cosmetic, run 48).

**12:26:50 — item 34's objective met.** Second resume: the phase-5 document task completed on its third attempt and wrote `audit/retrieval_risk_report.md` (4,817 bytes): 11 ranked findings (2 HIGH, 9 MEDIUM, ~18 LOW grouped), each with file:line evidence drawn from the four phase-3 artifacts (`GetTopFiles` negative-n panic at `tiered_context.go:644-656`, `locateFile` path traversal, unbounded `LoadContent`, cancel amplification, symlink escape…) and a seven-step fix order. The verify task passed it two seconds later on merit (`checkVerifyHollowReport` saw a 4.8 KB report against findings-bearing upstream). Compare the first campaign's 314-byte "No input content supplied". The report cites only phase 3 because the budget cut phases 0–2 (item 39).

**12:28:35 — campaign 5a2f4c8d ended `/failed: phase blocked: /all_tasks_blocked`** after the pathless duplicate task (item 40) hit its 4th attempt, with the phase's real work complete. Two more from the persisted state: **item 41** — `block_reason` is empty because the block came through `orchestrator_tasks.go:251-261`, not the `orchestrator_execution.go:196` site run 46 patched, so the new resume selection cannot see it without `--retry-failed`; **item 42** — at the attempt cap no failure-driven re-plan ran (attempts 1–3 logged "still retryable", attempt 4 went straight to block). Both in `brief_block_reason_paths.txt` (run 56: one `failCampaign(reason)` for every path; one re-plan per task at the cap before block). Report moved by hand from repo-root `audit/` to `.nerd/campaigns/5a2f4c8d/artifacts/retrieval_risk_report.md`; `audit/` removed. Runs 53 (tactile build env) and 55 (re-plan retype) dispatched 12:31.

**Items 38, 36 (readback half), 40 landed (~12:50):** tactile `BaseEnvironment` from `build.GetBuildEnv` (1f463e2b, run 53); executor owns the usage tracker, Spawner forwards it (811a7ca6, run 54 — it put the field on `SpawnerConfig` and spliced its comment into `TokenBudget`'s; repaired by hand, tests added by hand); re-plan output gets `applyTaskTypeDefenses` + `replanDedupeKey` (76b93151, run 55). Runs 54 and 55 ran concurrently in `internal/session` and `internal/campaign`: 55's `go build ./...` failed on 54's half-edited spawner.go — the run-34/35 lesson again, now with a sharper rule: **a package that imports the other run's package is not disjoint for verification purposes** (campaign imports session). Run 55 also left the new constructor parameter variadic "so existing call sites keep compiling" — a shim; run 57 (`brief_replanner_workspace_required.txt`) makes it required and repoints ~35 test call sites. Run 56 (`brief_block_reason_paths.txt`, items 41–42) follows in the same package. Run 57 landed (56579ec5).

**Item 43 (from `.nerd/campaigns/` before the composition relaunch):** eight campaigns from earlier sessions are persisted as `/active` or `/validating` although their processes are long gone (855d7bcf, f48c2c55, 6076f5e5, 6939b55b, 70ada911, fd28d6fc, 5f217212 …). Resume's selection (9e77ffd3) ranks active above failed-blocked, so a stale active record can shadow a genuinely blocked campaign. Fix shape: the orchestrator writes a liveness marker (PID + heartbeat timestamp) into the campaign JSON while running, and resume treats an active campaign whose marker is stale as paused. Not touched by hand — those records belong to other sessions. The composition goal doc is now versioned at `references/campaign-compose-2026-09-04.md`.

**Items 41–42 landed (run 56):** one `failCampaign(reason)` for both block paths, `Task.ReplannedAtCap`, one-shot re-plan at the attempt cap before the block check. **Item 44 (composition, opened by that fix):** the re-plan at the cap is now Go-decided; the kernel's `replan_needed/2` (derived in BOTH `policy/campaign_planning.mg:76-93` and `policy/campaign_phases.mg:111-128` — duplicated rule sets) has no Go consumer left, only tests. Correct shape per the north star: policy derives `replan_needed(C, /attempt_cap_reached)` from the task's attempt facts, Go consumes `replan_needed` again and nothing else, and one of the two rule copies is deleted. A seam-S7-class item for the composition campaign, not a hand fix.

## Composition campaign relaunched — codeNERD recursing over itself

**13:24, campaign aa202a5c** ("Restore Composition: One Source of Truth per Decision"), binary ed4dcf27, `--type remediation --timeout 12h`, source docs `references/campaign-compose-2026-09-04.md` + `composition-map-2026-09-03.md`. Plan (confidence 0.84, kernel-validated): 7 phases / 47 tasks mirroring the goal doc — Phase 0 verifies the "Already landed" section and reproduces break (a); Phase 1 S2 then S1; Phase 2 S7/S11 (`phase_complete`/`campaign_complete` consumers); Phase 3 S3/S5/S6 (detectors, incremental scan, holographic); Phase 4 S4/S10; Phase 5 S8/S9/S12 plus deletion of the ten redundant tracker-tagging sites; Phase 6 measure and report. Seven report tasks retyped `/file_modify`→`/file_create` at plan time (the run-24 defense). Watch: `nerd campaign status`; log `.nerd/logs/*_da47f3_campaign.log`; resume with `nerd campaign resume --timeout 12h`. Everything it needed from this pass is in the binary: Cortex boot, transitive upstream artifacts, verdicts that reach the kernel, metered turns, tactile children that can build cgo, re-plan defenses, block reasons persisted. Open items it will meet: 39 (budget spreading), 43 (stale active records), 44 (`replan_needed` orphan).

**Phase 0 completed 13:52 (22 min, 6/6, on merit):** `Checkpoint PASSED: /builds` — first live proof that tactile children compile the cgo packages (item 38); `turn_cost … prompt=563503 completion=14967 tools=45 outcome=/done` and three more non-zero turns — item 36's readback half proven (run 54); one turn `outcome=/hollow` at 13:49 caught by the verdict policy, task retried, phase still 6/6. The map-verification task graded the composition map's cited lines (ACCURATE / OFFSET / STALE / UNVERIFIED) and recorded that the `apply_edits` gate mapping was fixed today. Baseline denominator: ~2.4 M prompt tokens for six research tasks — the number the campaign is meant to reduce.

**Phase 1 (from 13:53) — the campaign's first self-edits:** re-plan took 35 s (the 7 min this morning was provider load). By 14:21 the tree carried S2 (`policy/interactive_gate.mg`, executor/router renames) and S1 (`policy/tool_capability.mg` + test) in progress, plus a repo-root `reports/` directory (the report-path litter again). Task 1_2's coder attempt broke exactly one test — `TestToolRegistry_RegisterToolWithInfo` expects 2 `tool_capability` facts and now sees 25, because the new policy table seeds the kernel — and the repair round did not fix it, so the orchestrator fell back to the direct-LLM path (which writes without the coder's test loop; the phase-end `/builds` + `/tests_pass` checkpoints are the catch). Mid-task the tree did not build (`interactivePendingActionPredicate` undefined — the rename in flight). Not touching any of it: the campaign is meant to find and fix this; if the phase blocks on the registry test after the campaign pauses, that test is a hand fix (it pins the pre-S1 world).

**Item 45 (14:21–14:55):** phase 1's file tasks (1_2, 1_4, 1_5) each failed their micro-checkpoint on build errors from *another* task's in-flight edit in the same phase (`spawner.go:38 undefined`, then the registry test) — concurrent file tasks share one working tree, so one task's half-done rename fails every sibling's verification. Fix shape: per-task worktrees or serialize file tasks whose write sets share a package.

**14:56 — re-aimed.** Steve clarified what composition means: the harness composing fluidly so that on any coding task, ideation through hardening, the agent gets the right context at the right time for the right thing (SWE-bench as the external yardstick for the fix slice). My goal doc had framed it as internal decision ownership — plumbing, not the outcome. Paused aa202a5c cleanly (`nerd campaign pause`, status `/paused`, resumable), stashed its unverified S1/S2 edits (`git stash list`: "campaign aa202a5c phase 1 in-flight"), moved its repo-root `reports/` under its artifacts. New goal doc `references/campaign-lifecycle-2026-09-04.md`: phase 0 closes the context-feedback loop (the feedback store learns per-predicate usefulness but is wired only on the TUI path — `cmd/nerd/chat/process.go:903`; the session executor logs the block at `executor.go:1830-1838` and drops it, same shape as the hydration gap fixed this morning), then lifecycle stage as a kernel fact with stage-shaped context policy, golden tasks per stage with a baseline, the bug-fix slice (retrieval report's eleven findings + SWE-bench Lite wiring), implementation/verification and hardening slices, re-measure. The twelve seams become plumbing pulled in where a stage measurement says so.

**14:58 — lifecycle campaign 149c512d launched** ("Lifecycle Context Precision — Right Context at Right Time", binary ed4dcf27, 12 h, docs: lifecycle goal doc + retrieval risk report). Plan: 6 phases / 45 tasks, kernel-validated — 0 scaffold and already-exists verification; 1 kernel facts (`context_feedback`/helpful/noise/missing Decls, `task_stage` as a Mangle table) and stage-shaped context policy; 2 retrieval hardening (P1 `GetTopFiles` … with pinning tests); 3 executor feedback loop + `missing_context` resolver; 4 golden tasks via `chat_driver.py` + SWE-bench wiring; 5 re-measure and report. The decomposer moved the golden-task baseline after the changes (the doc had it before); accepted — this repo's before-numbers are in today's archived logs and the SWE-bench before-number never existed. Eleven report tasks retyped to `/file_create` at plan time.

**Phase 0 done on merit (15:12, `/builds` passed). Phase 1 (15:13–16:16):** run 55's re-plan defense fired live — four pathless tasks the refinement added were retyped to `/research` (item 40 proven). It wrote the four `context_*` Decls, a `task_stage` table, brainstorm and design intent atoms. Then **item 46, the worst find of the day:** the `/tests_pass` checkpoint reported 3,584 failures — task 1_1 (`/file_modify` on `schemas_execution.mg`): the coder shard finished without writing, the hollow gate correctly failed it, and `executeFileTaskFallback` (`orchestrator_task_handlers.go:449`) generated whole-file content from a "Generate the following file:" prompt and wrote it with **`os.WriteFile` (line 538)** — never reading the existing file, never distinguishing modify from create, bypassing the VirtualStore, `permitted/3`, the write gate, the Dreamer gate and the FileWriteValidator. The 251-line execution schema became 9 lines; every kernel-booting test panicked. **The VirtualStore is the only door to the world and this fallback was a second door.** Paused the campaign (`/paused`), restored the schema from HEAD plus the campaign's four new Decls by hand (kernel tests green), and dispatched run 58 (`brief_fallback_front_door.txt`: refuse the fallback when the target exists, route new-file writes through the VirtualStore, guard the artifacts write). Three other fallbacks created new files, one in an invented `internal/policy/` package that no loader reads — **item 47:** fallback- and decomposer-chosen paths are not validated against the repo contract (policy loads only from `internal/core/defaults/policy/`). **Item 48 — answered by `turn_cost`:** the coder turn on task 1_1 ran 12 LLM calls in 12m45s at `prompt=4921650` tokens (~410 K per call) and ended `/hollow`; the research turn before it ran 35 calls at ~126 K each (4.4 M). The coder is not short of context, it is drowning: whole-file reads plus 46 KB of injected artifacts accumulate in the tool-loop history (`context_window.max_tokens` is 1 M in the user config, so nothing compresses until far too late), the model spends its whole budget reading and never reaches the edit. This is the lifecycle campaign's thesis measured in its own first phase: right context at the right time is currently the wrong amount at every time, at millions of tokens per task. Seam S4 (budgets as structure — shrink the tool list and evict/summarize tool results as the budget drains) and the feedback loop's `noise_facts` are the levers; the campaign's phase 3/4 measure them. Not changing the user's 1 M window by hand.

**Routing root cause found by that run (item 33):** the plan carried four bogus `missing_category` topology warnings. Not the policy, not the fact shapes (a reproduction with real `/phase_…` IDs on a fresh kernel derives precedence fine — `internal/core/campaign_phase_category_test.go`). The Cortex kernel routes facts by predicate owner, the `campaign` shard owns `campaign_phase` but not `phase_category`, so the topology rule evaluates in a shard that never sees the category. The private campaign boot's single kernel had masked it; `per_shard_facts` (`false` in config) is read only by its own tests, so routing is always on. Runs 42/43 fix both halves.

Unconfirmed: run 30 reported "shell gate blocked go vet/go test"; no gate refusal appears in its logs. Most likely the 60 s timeout (item 26).

**Live probe 1 (05:58, `nerd chat`, two turns):** both turns correct (file read + one-sentence answer 33 s; recall of the prior turn 35 s). Real API cost of turn 1 was ~13K in / 1.4K out on the tool-results call, i.e. the harness is cheap; the reported 455K was the cross-process accounting bug (item 23).

**Live probe 2 (07:45, same two turns, binary 2c132b51):** hydration runs on every process now — `HydrateLearnings completed: 9424 facts` once (1.2 s), `HydrateSessionContext` 5–6 facts per turn (3–30 ms) — and `turn_cost` reads `prompt=26497 completion=3631 tools=1` for the tool-using turn and `prompt=12662 completion=2073 tools=0` for the recall turn, matching the API calls. Both answers correct; turn 1 took 60 s with two fix runs sharing the provider, turn 2 24 s. **Open:** `outcome=/unverified` on both — `turn_done` is derived only for write verbs, so a read-only turn with successful tool evidence has no positive verdict; the denominator cannot yet count read-only work as verified.

**Machine note:** another session works in `C:\CodeProjects\neurolog` on this box (foreman runner adapters, hook scripts, a Grok run with a 3 h budget). Under that load a bash spawn takes 4–9 s, so the shell tool's test package (dozens of spawns) exceeds a 4 min `go test` timeout while passing in 17 s when quiet. Not a hang.

## Open, ranked (one concern each)

**Turn loop**
- `SetSessionID` never called in the factory: every non-`nerd chat` turn persists under `"default"` (`internal/system/factory.go` ~1610).
- `SetOuroborosRegistry` wired only in the TUI; `executor.go:513` panics on nil (`factory.go` ~1616).
- `ConfigFactory.Generate` never calls `Validate()`; its `ToolLoop` 5/50 budget is inert and contradicts `DefaultExecutorConfig` (`internal/prompt/config_factory.go:148-157`).
- Memory is write-only on the executor path: `HydrateLearnings`/`HydrateSessionContext` chat-only; `atomsJSON` hardcoded empty; `memory_operation/3` has no reader; `context_feedback` and `intent_classification` from the Piggyback packet are log-only. `turn_cost/6` still unimplemented.
- `internal/context` (compressor, activation) not wired outside the TUI.
- `next_action/1` never consulted by the executor (deliberate; documented).

**Kernel / shards**
- `tactile_router` never spawned in production; the second pipeline is dormant by construction — decide: spawn it or delete the emission.
- `ExecutivePolicyShard` retracts `user_intent`/`pending_action` on every start (destructive on mid-session restart).
- ~~`diff_eval` incremental eval dead; evals cost 14–24 s~~ — wrong for production. `.nerd/config.json` sets `diff_eval: true`; the 04:23 kernel log shows 105 evaluations via the differential path, 0 full, ~60–70 ms wall each (912 strata). The 14–24 s figure is the test-time full path (compile-time default off). Only the stale comment in `kernel_eval.go:26-38` was real; corrected. `per_shard_facts` is still off by config (deliberate, pinned by test), so the 7-shard manifest is inert data.
- Shadowed `tactile_router`/`campaign_runner` factories in `registration.go:302-319`.

**Campaign / autopoiesis**
- `nerd campaign start` and `resume` each hand-assemble their own kernel, shard manager and VirtualStore (`cmd_campaign.go` ~242 and ~830) instead of booting the Cortex like every other command — a fourth and fifth boot path. This is *why* `ToolPregenerator` is nil (~650 lines dead), the intelligence gatherer gets no tool generator or MCP store, and every factory fix has to be mirrored by hand. Brief written: `brief_campaign_boot.txt`.
- `TaskVerifier` fails open: a verifier error is recorded as success at confidence 0.3 and a nil client as 0.5 (`internal/verification/verifier.go` ~224-232, ~283). Brief written: `brief_verifier_failopen.txt`. The TUI compares the max-retries error by string (`process.go:515`).
- `ExecutivePolicyShard` retract of `user_intent`/`pending_action` happens in Start (`executive.go:427-434`) and is correct at boot; it is destructive only if the shard restarts mid-session. Leave until a restart path exists.
- MCP `JITToolCompiler`/`CompileToolsForShard` orphan is inert here: `.nerd/config.json` has `integrations.servers: {}`. Low priority.
- `next_action(/campaign_*)` derived, never consumed (orchestrator dispatches through Go).
- Verifier (`VerifyWithRetry`) reachable only from the TUI.
- `prompt_evolved`/`thunderdome_result` have no producer; `correction_pattern` has no consumer.
- Windows hot-reload holds the tool binary (`TestOuroborosLoop_HotReload_LockedBinary`).

**Effects / memory**
- Retrieval semantic tier (`NewEmbeddingSemanticSearcher`) has no production caller.
- No workspace rescan on `nerd run/fix/create/review`; campaign builds a second default scanner.
- Embedding degradation announced once at boot, then invisible.
- MCP `JITToolCompiler`/`CompileToolsForShard` orphan: MCP tools are callable but never described to the model.
- `review_findings` write-only; `task_verifications` readers uncalled.
- `Registry.SetAllowlist` never set (by design; direct `tools.Global().Execute` is gated only by the write guard).

**Control surface**
- `nerd fix X` ≠ TUI "fix X" (no perception/routing/verification); `nerd chat` ≠ TUI (`SystemComponents.SessionExecutor` never read by the Model); `nerd run` is a third loop.
- `--dry-run`/`--dump-kernel`/`--trace-api` registered, unread; `--verbose` bound twice with different meanings; `--disable-system-shard` only on `run` but read everywhere.
- Continuation steps use `context.Background()` (Ctrl+X cannot cancel); `launchClarifyAnswers` and `awaitingKnowledge` written on value receivers.
- `main.go` loads `config.yaml`, not `config.json`, so `GetExecutionTimeout` never applies.
- `performSystemBootLegacy` (~1050 lines) dead.
- Config inert: `guidance.*`, onboarding tour fields, `logging.file`; six `llm_timeouts` fields Z.AI-only (Gemini/Anthropic/OpenAI/compat hardcode 10 min).
- TransparencyManager TUI-only; `sqlpragmas` host-class and metrics, `internal/build` helpers, `ux.GetDisclosureLevel`, `transparency.QuickExplain` unused.
