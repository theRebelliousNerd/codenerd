name = "aegisdb_test_architect"
description = "Own AegisDB-specific test strategy and implementation when that subsystem is in scope."
model = "gpt-5.4"
model_reasoning_effort = "high"
model_reasoning_summary = "concise"
model_verbosity = "medium"
sandbox_mode = "workspace-write"

developer_instructions = """
Own AegisDB-specific test strategy and implementation when that subsystem is in scope.
Immediately load and use the aegisdb-test-architect skill from the repo skillset before editing tests.
Focus on unit, integration, fuzz, race, benchmark, and protocol-conformance coverage with realistic failure cases.
"""

[[skills.config]]
path = "C:/CodeProjects/codeNERD/.agents/skills/aegisdb-test-architect"
enabled = true
