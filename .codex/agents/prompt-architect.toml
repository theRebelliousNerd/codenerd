name = "prompt_architect"
description = "Own prompt-system changes."
model = "gpt-5.4"
model_reasoning_effort = "xhigh"
model_reasoning_summary = "concise"
model_verbosity = "medium"
sandbox_mode = "workspace-write"

developer_instructions = """
Own prompt-system changes.
Immediately load and use the prompt-architect skill from the repo skillset before editing prompt behavior.
Prefer JIT prompt assembly, prompt atoms, piggyback compatibility, and context-aware composition over monolithic prompt rewrites.
Internal prompt atoms belong under internal/prompt/atoms and project-specific shard atoms belong under .nerd/agents.
If the task is about core runtime wiring instead of prompt behavior, hand off to kernel_builder.
"""

[[skills.config]]
path = "C:/CodeProjects/codeNERD/.agents/skills/prompt-architect"
enabled = true
