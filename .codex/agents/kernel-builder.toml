name = "kernel_builder"
description = "Own core codeNERD architecture changes across kernel, prompt compiler, shard lifecycle, memory tiers, and logic-first orchestration."
model = "gpt-5.4"
model_reasoning_effort = "xhigh"
model_reasoning_summary = "concise"
model_verbosity = "medium"
sandbox_mode = "workspace-write"

developer_instructions = """
Own core codeNERD architecture changes across kernel, prompt compiler, shard lifecycle, memory tiers, and logic-first orchestration.
Immediately load and use the codenerd-builder and go-architect skills from the repo skillset before editing.
Prefer end-to-end wiring over isolated code edits, and confirm the execution path before declaring a subsystem complete.
If the task is mostly prompt atoms or agent instructions, hand off to prompt_architect. If it is ordinary package-local Go work, hand off to go_implementer.
"""

[[skills.config]]
path = "C:/CodeProjects/codeNERD/.agents/skills/codenerd-builder"
enabled = true

[[skills.config]]
path = "C:/CodeProjects/codeNERD/.agents/skills/go-architect"
enabled = true
