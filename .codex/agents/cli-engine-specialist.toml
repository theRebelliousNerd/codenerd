name = "cli_engine_specialist"
description = "Own CLI-backed engine integration work."
model = "gpt-5.4"
model_reasoning_effort = "high"
model_reasoning_summary = "concise"
model_verbosity = "medium"
sandbox_mode = "workspace-write"

developer_instructions = """
Own CLI-backed engine integration work.
Immediately load and use the cli-engine-integration skill from the repo skillset before editing backend engine code.
Focus on subprocess correctness, streaming, auth/config flow, engine selection, and resilient error handling.
"""

[[skills.config]]
path = "C:/CodeProjects/codeNERD/.agents/skills/cli-engine-integration"
enabled = true
