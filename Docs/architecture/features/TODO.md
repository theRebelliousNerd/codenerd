# TODO — features

> Last verified against codebase: **2026-08-15**  
> Docs-only backlog (this corpus does not implement code).

## P1 — Wiring

- [x] Wire `cmd/tools/verify_taxonomy` to `features.IsTaxonomyFastEnabled()` (and ensure SetActive/env path consistent with resolveBool, not only `== "1"`).
- [x] Align comments: remove “hard short-circuit” language for PerShardFacts where accessor is normal resolveBool; update `kernel_eval.go` DiffEval default claim; fix SystemShards field env comment.

## P2 — Operability

- [x] Improve `Summary()` to print resolved booleans (dereference `*bool` or log `Is*` snapshot) so Boot logs are human-readable.
- [x] Optional CLI: `nerd features` or status subsection listing resolved flags (env vs active vs default source).
- [x] Optional chat slash `/features` mirroring Summary — `cmd/nerd/chat/commands_handlers_features.go`, registered in `CommandRegistry` so `/help` lists it. Renders `Resolved()` as a table plus the exact `Summary()` line Boot logs, and warns about deprecated env vars.

## P3 — Consistency

- [x] Env prefix migration (`NERD_*` → `CODENERD_*`) — dual-read implemented for the four legacy names; canonical wins, `Source` distinguishes `legacy-env`, `Deprecations()` reports the rest. Removal criterion recorded in the package doc and 06-PUBLIC-API §9; `TestEnvMigration_LegacyVarsShouldBeTheKnownFour` fails loudly if the set changes.
- [x] JSON schema snippet for the `features` block — generated by `features.ConfigSchemaJSON()`, printed by `nerd features --schema`, embedded in 06-PUBLIC-API §1.1. Generated rather than transcribed so it cannot drift; `TestConfigSchemaJSON_ShouldListEveryRecognisedKey` fails on an undocumented flag.

## P4 — Testing

- [x] Table-driven precedence matrix for all eight boolean accessors.
- [x] Summary format test once Summary is fixed.
- [x] Optional `-race` concurrent SetActive stress.

## P5 — Product / Track D

- [x] PerShardFacts — audited 2026-08-15, **documented as continued opt-in**. The
  stated blocker (manifest not auto-wired into `internal/system/factory.go`) is
  RESOLVED: `defaultKernelShardConfigs` builds every shard config from
  `shards.DefaultShardPredicateManifests`. The real blocker remains:
  `ShardFactRouter` routes a single predicate to its owner but does not evaluate
  joins across shards, so a rule body spanning two owners derives nothing.
  Flipping the flag would silently delete cross-domain derivations. Pinned by
  `TestPerShardFacts_ShouldRemainOptInEvenWhenFullyEnabled` and
  `TestPerShardFacts_ShouldStillHonourAnExplicitOptIn`; reasoning in
  `FullyEnabledFeaturesConfig` and 06-PUBLIC-API §10. Re-open when a
  cross-shard join coordinator exists.

## Done (do not re-open without evidence)

- Leaf package extraction for cycle break  
- env > active > default precedence  
- SetActive snapshot copy  
- LoadUserConfig install + Boot log  
- Conservative DefaultFeaturesConfig  
- FullyEnabled seed with PerShardFacts false  
- Config round-trip external tests  
