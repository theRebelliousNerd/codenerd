# Every production tool that reaches Go source text without an element
# address (TestRawTextRoutes_OnlyShrink). This list may only shrink: a tool
# that becomes element-addressed is deleted from it, and no new line is added.
# Seeded 2026-09-23 from the probe's own classification.
bash exec
delete_file raw-write
edit_file raw-write
git_diff raw-read
git_operation exec
grep raw-read
read_file raw-read
run_command exec
search_expand raw-read
