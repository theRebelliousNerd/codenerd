# atom_has_lang_match / atom_has_framework_match were asserted here until
# 2026-09-09. Both rules were removed from jit_logic.mg: nothing produced their
# input (atom_selector) outside tests, and nothing consumed their output at all.
# This golden file was the only thing keeping them green.
atom_matches_context("/atom_core", 100)
atom_matches_context("/atom_go", 90)
atom_matches_context("/atom_react", 90)
atom_dependency_satisfied("/atom_core")
atom_dependency_satisfied("/atom_go")
atom_dependency_satisfied("/atom_react")
